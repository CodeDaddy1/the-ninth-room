---
description: Create thumbnail / cover / on-screen concepts → work/<slug>/visuals.json
argument-hint: "slug=<slug>"
---

Use the **visual-director** subagent.

Arguments: $ARGUMENTS

Required: `slug`. If missing, ask first. Read `work/<slug>/script.md` to learn the
hook and beats, then **Write** `work/<slug>/visuals.json` per the agent's schema,
using only the locked brand tokens (do not invent colors or type). Lead the
summary with the file path.
